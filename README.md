# Dv1663-Sql
Modules needed to be imported:

1.mysql.connector Version: 2.2.9

Home-page: http://dev.mysql.com/doc/connector-python/en/index.html

Author: Oracle and/or its affiliates

to install:  pip install mysql-connector-python

2.csv




Some of the python code is hardcoded to only work with the current data sets and should more data such as a movie be added it might not show up as a choice depending on what query choice is made. 

Some of the functions used in the mysql.connector library are depreciated and will show an error upon execution however it is still functional. 

There is no error/exception handling in the queries so should one for example use a string instead of an int as an input the program might crash.
